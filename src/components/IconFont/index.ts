import { createFromIconfontCN } from '@ant-design/icons';

import { ICONFONT_URL } from '@/constants/icon';

const IconFont = createFromIconfontCN({
  scriptUrl: [ICONFONT_URL],
});

export default IconFont;
